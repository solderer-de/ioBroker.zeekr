name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
        working-directory: .
      - name: Run tests
        run: npm test
        working-directory: .
      - name: Create release archive
        run: tar -czf zeekr-adapter-${GITHUB_REF_NAME}.tar.gz .
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: zeekr-adapter-${{ github.ref_name }}.tar.gz
