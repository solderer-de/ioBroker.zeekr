{
  "name": "iobroker-adapter-zeekr",
  "version": "0.1.0",
  "description": "ioBroker adapter for Zeekr electric vehicles",
  "private": true,
  "main": "main.js",
  "scripts": {
    "test": "NODE_ENV=test node --test",
    "lint": "echo 'No linting configured yet'"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@iobroker/adapter-core": "^3.3.0",
    "@iobroker/testing": "^5.0.0"
  }
}
